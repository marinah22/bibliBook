export interface Book {

        title: string;
        genre:string;
        auteur:string;
        isbn: string;
        resume?: string;
        date: string 
        loc?: boolean;
        photo?: string;

        
}
